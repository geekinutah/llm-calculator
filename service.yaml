apiVersion: v1
kind: Service
metadata:
  name: llm-calculator
  namespace: default
  labels:
    app: llm-calculator
spec:
  selector:
    app: llm-calculator
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
